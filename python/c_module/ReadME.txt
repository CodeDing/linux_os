$sudo python setup.py install
$sudo apt-get install ipython
$ipython
>>>import ext
>>>def compare(a,b):
	if(a > b):
		return -1
	if(a < b):
		return 1
	if(a == b)
		return 0
>>>ext.setcmp(compare)
>>>ext.compare("a","b")

